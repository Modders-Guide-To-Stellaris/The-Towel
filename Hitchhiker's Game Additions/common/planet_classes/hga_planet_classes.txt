pc_hga_ecology = {
    entity = "hga_ecology_planet"
    entity_scale = @planet_standard_scale
    icon = GFX_planet_type_gaia
    icon_large = GFX_planet_type_gaia_big

    auto_trait_prio = {
        trait_auto_pc_gaia_preference
    }

    atmosphere_color		= hsv { 0.58	0.3 0.7 }
    atmosphere_intensity	= 1.0
    atmosphere_width		= 0.5

    city_color_lut = "gfx/portraits/misc/colorcorrection_tropical.dds"

    modifier = {
        planet_jobs_produces_mult = 0.20
        pop_happiness = 0.10
        logistic_growth_mult = 0.15
        planet_resettlement_unemployed_destination_mult = 0.5
        pop_environment_tolerance = 1.5 ## in order to avoid overwriting 5 bajillion traits, everyone gets 50% more habitability
    }

    spawn_odds = 0

    colonizable = yes
    district_set = hga_ecology_world
    ideal = yes
    starting_planet = no
    uses_alternative_skies_for_moons = yes
    uses_alternative_skies_if_has_orbital_ring = yes

    carry_cap_per_free_district = @carry_cap_high
}
