room_selector = {
    #default = "d_room"	#point to textures that end with "_room.dds" but remove the file ending
    default = "hga_paragon_skrand_room"	#point to textures that end with "_room.dds" but remove the file ending
    #will run with a limited country scope. species and government is set but the country does not actually exist

    # FNote: In an attempt to not 'break things' I've opted to reimport the paragon textures :P

    game_setup = {
        "hga_paragon_azaryn_room" = { always = yes }
        "hga_paragon_skrand_room" = { always = yes }
        "hga_paragon_keides_room" = { always = yes }
    }

    #leader scope
    ruler = {
        "hga_paragon_azaryn_room" = {
            exists = owner
        }
        "hga_paragon_skrand_room" = {
            exists = owner
        }
        "hga_paragon_keides_room" = {
            exists = owner
        }
    }
}