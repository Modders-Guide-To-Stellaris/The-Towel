hga_zone_food_arcology = {
    inline_script = {
        script = zones/shared_food_zone
        AMOUNT = @doubled_scaling_district_1_job
    }

    swap_type = hga_district_food_arcology
    swap_type_weight = 5

    zone_sets = {
        arcology
    }

    potential = { # planet scope
        has_global_flag = hga_arcology_menials_in_play

        # planet = {
        #     NOT = {
        #         has_district = hga_district_food_arcology
        #     }
        # }
    }

    included_building_sets = {
        urban_automation_arcology
    }

    excluded_building_sets = {
        urban_automation
    }

    inline_script = {
        script = zones/shared_arcology_zone_modifiers
    }
}

hga_zone_energy_arcology = {
    inline_script = {
        script = zones/shared_energy_zone
        AMOUNT = @doubled_scaling_district_1_job
    }

    swap_type = hga_district_energy_arcology
    swap_type_weight = 5

    potential = { # planet scope
        has_global_flag = hga_arcology_menials_in_play

        # planet = {
        #     NOT = {
        #         has_district = hga_district_energy_arcology
        #     }
        # }
    }

    zone_sets = {
        arcology
    }

    included_building_sets = {
        urban_automation_arcology
    }

    excluded_building_sets = {
        urban_automation
    }

    inline_script = {
        script = zones/shared_arcology_zone_modifiers
    }
}

hga_zone_mineral_arcology = {
    inline_script = {
        script = zones/shared_minerals_zone
        AMOUNT = @doubled_scaling_district_1_job
    }

    swap_type = hga_district_mineral_arcology
    swap_type_weight = 5

    potential = { # planet scope
        has_global_flag = hga_arcology_menials_in_play

        # planet = {
        #     NOT = {
        #         has_district = hga_district_mineral_arcology
        #     }
        # }
    }

    zone_sets = {
        arcology
    }

    included_building_sets = {
        urban_automation_arcology
    }

    excluded_building_sets = {
        urban_automation
    }

    inline_script = {
        script = zones/shared_arcology_zone_modifiers
    }
}