# common/on_actions/hga_on_actions.txt
# Authors: HGA
# >- HOST MENU ON ACTIONS -<

on_game_start = {
	events = {
		hga_internal.1
	}
}

# Game Start >> fires hga_menu.0 (init + open menu) for the host
# this = country
on_game_start_country = {
	events = {
		hga_menu.0		# Host Menu >> init defaults and open menu for host
	}
}

# Do not remove below v
