# common/scripted_triggers/hga_scripted_triggers.txt
# Authors: HGA

# Relic CB shared validity — scope: country, from = target country
	hga_can_seize_relic_from = {
		is_country_type = default
		NOT = { is_overlord_to = from }
		OR = {
			is_subject = no
			overlord = { NOT = { is_same_value = from } }
		}
		has_intel_level = {
			who = from
			category = economy
			level >= 2
		}
	}
# /

# Do not remove below v
