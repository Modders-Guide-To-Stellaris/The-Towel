# common\deposits\hga_deposits.txt
# Authors: Curator Foxy

# >- BEAUTY PLANET CRYSTALLINE DEPOSIT -<

	d_hga_crystalline_wonder = {
		is_for_colonizable = yes
		category = deposit_cat_rare_resource

		icon = "GFX_deposit_d_crystal_reef"

		triggered_planet_modifier = {
			potential = {
				exists = owner
				owner = {
					has_technology = tech_mine_rare_crystals
				}
			}
			modifier = {
				planet_miners_rare_crystals_produces_add = 0.25
			}
		}

		planet_modifier = {
			district_mining_max_add = 2
		}

		drop_weight = {
			weight = 0
		}
	}

# /

# >- RECONSECRATED BEAUTY - HOLY WORLD UNITY DEPOSIT -<

	d_hga_beauty_holy_unity = {
		is_for_colonizable = yes
		category = deposit_cat_rare

		icon = "GFX_deposit_d_crystal_reef" #todo: change

		resources = {
			category = orbital_mining_deposits
			produces = {
				trigger = {
					has_global_flag = hga_features_intensity_low
				}

				unity = 10
				mult = planet.owner.trigger:num_ascension_perks
			}

			produces = {
				trigger = {
					has_global_flag = hga_features_intensity_medium
				}

				unity = 20
				mult = planet.owner.trigger:num_ascension_perks
			}

			produces = {
				trigger = {
					has_global_flag = hga_features_intensity_high
				}

				unity = 30
				mult = planet.owner.trigger:num_ascension_perks
			}
		}

		station = shipclass_research_station

		drop_weight = {
			weight = 0
		}
	}

# /

# Do not remove below v
